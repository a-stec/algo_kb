"""
Задачи на применение идей быстрой сортировки

Задача 1. Поиск k-го наименьшего элемента (Quickselect)
Цель: применить технику разбиения для поиска порядковой статистики без полной сортировки.
Напишите функцию quickselect(arr, k), которая находит k-й наименьший элемент в неотсортированном массиве (k от 1 до n). 

Используйте подход, аналогичный быстрой сортировке:
- Выберите опорный элемент (например, случайный).
- Разделите массив на две части: элементы меньше pivot и больше pivot.
- Если в левой части ровно k-1 элементов, то pivot — искомый.
- Если в левой части больше или равно k элементов, рекурсивно ищите в левой части.
- Иначе ищите в правой части, скорректировав k.

Требования:
- Нельзя сортировать весь массив.
- Ожидаемая сложность O(n) в среднем.

Задача 2. Группировка отрицательных и положительных чисел
Дан массив целых чисел, содержащий как отрицательные, так и положительные значения (ноль считаем положительным или нейтральным). Необходимо переставить элементы так, чтобы все отрицательные числа оказались слева, а неотрицательные — справа. Порядок внутри каждой группы не важен.

Реализуйте функцию rearrange_signs(arr), используя однопроходный алгоритм, аналогичный partition (можно взять схему Ломуто или Хоара, но без опорного элемента — просто разделить по знаку). Оцените сложность по времени и памяти.
"""
